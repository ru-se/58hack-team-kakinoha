services:
  # --- 既存のFastAPIサーバー ---
  engineer-hub:
    build: .
    container_name: engineer-hub-server
    env_file:
      - .env
    privileged: true
    ports:
      - "8000:8000"
    volumes:
      - /var/run/dbus:/var/run/dbus
    restart: always